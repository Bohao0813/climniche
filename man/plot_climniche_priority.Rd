% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priority.R
\name{plot_climniche_priority}
\alias{plot_climniche_priority}
\alias{plot.climniche_priority}
\title{Plot ecological screening of climate exposure}
\usage{
plot_climniche_priority(
  x,
  type = c("plane", "map", "both"),
  map_value = c("pareto_depth_score", "pareto_rank", "relative_priority"),
  max_points = 6000L,
  seed = 1L,
  title = NULL,
  extent = NULL,
  degree_labels = c("auto", "none", "hemisphere"),
  study_region = NULL,
  legend_position = "bottom"
)

\method{plot}{climniche_priority}(x, ...)
}
\arguments{
\item{x}{A \code{climniche_priority} object.}

\item{type}{\code{"plane"}, \code{"map"}, or \code{"both"}.}

\item{map_value}{Map \code{"pareto_depth_score"}, the original \code{"pareto_rank"},
or the legacy alias \code{"relative_priority"}.}

\item{max_points}{Target maximum number of cells drawn in the decision plane.
Pareto rank 1 is always retained.}

\item{seed}{Random seed used when the decision plane is subsampled.}

\item{title}{Optional overall title for a combined figure.}

\item{extent}{Optional \code{c(xmin, xmax, ymin, ymax)} map extent.}

\item{degree_labels}{Longitude-latitude label style.}

\item{study_region}{Optional study-region boundary accepted by
\code{\link[=plot_climniche_map]{plot_climniche_map()}}.}

\item{legend_position}{Legend position for the map.}

\item{...}{Additional arguments passed to \code{plot_climniche_priority()} by the
S3 \code{plot()} method.}
}
\value{
A ggplot object, a patchwork object, or a named list of plots when
\code{patchwork} is unavailable.
}
\description{
Plot ecological screening of climate exposure
}
\examples{
sim <- simulate_climniche(n = 400, p = 6, seed = 21)
fit <- fit_climniche(
  sim[["current"]],
  sim[["future_away"]],
  occupied = sim[["occupied"]],
  sensitivity = sim[["sensitivity"]]
)
priority <- climniche_priority(fit)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_climniche_priority(priority, type = "plane")
}
}
